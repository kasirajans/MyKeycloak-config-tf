# AIAgent Client Credentials - Example Template
# Copy this to .env and populate with actual values from Terraform output
# Run: ./update-test-credentials.sh aiagent_MCP_oktaAPI

# Keycloak Configuration
KEYCLOAK_URL=http://localhost:8080
REALM=AIAgent

# Client Credentials (populate from Terraform output)
CLIENT_ID=your-client-uuid-here
CLIENT_SECRET=your-client-secret-here
SCOPE=okta-api-access

# Token endpoint (auto-computed)
TOKEN_URL=${KEYCLOAK_URL}/realms/${REALM}/protocol/openid-connect/token
